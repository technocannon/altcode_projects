import React from 'react';
import {Text} from "native-base";

const AccountScreen = () => {
    return (
        <Text>
            Hello from Wishlist
        </Text>
    );
};

export default AccountScreen;