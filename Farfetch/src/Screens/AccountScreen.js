import React from 'react';
import {Text} from "native-base";

const AccountScreen = () => {
    return (
        <Text>
            Hello from Account Screen
        </Text>
    );
};

export default AccountScreen;